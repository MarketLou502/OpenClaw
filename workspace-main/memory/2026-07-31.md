# 2026-07-31

- Migrated the Zaxby's receipt conversation/state layer from the former inbound
  compatibility layer to Main's native iMessage architecture. The existing
  Playwright browser runner remains under Main. The retired routing files were
  later moved to the dated legacy archive.
- Aaron chose the exact same-message trigger `Zaxby survey`; bare photos must
  never trigger the automation.
- Main attempts OCR. A structurally valid high-confidence result may submit
  immediately because the trigger phrase is explicit intent. An uncertain
  result is staged for ten minutes and requires exact `yes` or a corrected
  15-character code.
- Added `scripts/zaxbys-workflow.js`, isolated tests, and
  `ZAXBYS_WORKFLOW.md`. The workflow validates codes, bounds errors, keeps
  per-conversation pending state, and prevents duplicate survey submissions.
- Internal fake-runner tests passed without opening the real survey or sending
  any iMessage. A real native-iMessage receipt remains the acceptance test.
- Aaron decided Sports Betting will remain unchanged on Discord. The intended
  final Discord footprint is the Sports Betting bot/account/binding only; all
  other bot conversations and notifications should migrate or be retired.
- Replaced the active Daily Tracker launchd heartbeat with Main's deterministic
  native-iMessage calendar notification workflow. Morning summaries now send
  only when events exist. Timed reminders exclude the exact six routine titles
  Guitar, Golf, Spanish, Study AI 900, Clean, and Workout/Run, plus explicitly
  no-reminder events. The superseded heartbeat was later archived.
- Added deterministic health activity tracking. Hourly micro-workouts use eight
  slots from 9 AM through 4 PM every day and update the bottom Health widget as
  `x/8`; an unanswered prompt stays silent. A short `yes` is valid only within
  70 minutes of the latest prompt. Daily run phrases default to 30 minutes and
  mark only the top `Workout/Run` goal. Both success replies are exactly
  `Got that logged.`
- Loaded `ai.openclaw.hourly-workout-prompt` with eight native-iMessage launchd
  triggers and restarted dashboard-api. Live API/static reads showed the new
  `hourlyWorkouts: 0/8` field and label. No synthetic exercise was logged; the
  next prompt and a natural run report are the remaining acceptance checks.
- Retired Finance from active OpenClaw agent/Discord configuration without
  deleting its workspace, database, scripts, or history. Removed its disabled
  Discord cron and all active Finance-script Discord delivery.
- Final Finance scope was narrowed to two dashboard values: dollars spent today
  from successful debit-purchase emails, and upcoming recurring transactions
  due during the remainder of the current month. Account balances, projections,
  aggregation providers, PIN access, and Finance notifications were retired.
- Aaron rejected a custom iMessage approval-code flow. A direct, unambiguous
  request such as deleting a specific calendar event authorizes the action;
  clarification is needed only when the target or requested change is unclear.
- Aaron approved retaining only two proactive notification systems: native
  iMessage calendar summaries/reminders and eight hourly workout prompts from
  9 AM through 4 PM. Generic Main system/agent checks and Health calorie,
  protein, missed-run, end-of-day, and extra-workout heartbeats were retired.
- Completed the final routing cutover: native iMessage is bound only to Main;
  Sports Betting is the only Discord account/binding; Daily Tracker and Health
  Tracker are internal specialists. FBB and the former raw-iMessage
  watcher/handler were moved into dated recovery archives.
- OpenRouter remains active. Its key and the retained Sports Betting token now
  live in private `~/.openclaw/.env`; `openclaw.json` contains environment
  references instead of literal secrets.
