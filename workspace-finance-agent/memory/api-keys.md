# API Keys — finance-agent

No active balance provider or financial aggregation credential is configured.

The retained Finance features—dollars spent today and upcoming recurring
transactions—use Capital One email alerts and local `finance.db` data only.
