# HEARTBEAT.md - Retired

Goals has no model-driven heartbeat and sends no proactive messages. It is a
delegated specialist only — Main owns the only user-facing reply, and the
hourly workout prompts are Main's deterministic workflow, not this agent's.
