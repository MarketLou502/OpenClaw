# 2026-08-07 - Nightly Routing QA Review

## Status: BLOCKED

**Time:** 2026-08-07 16:13 UTC (Friday 12:13 PM EDT)  
**Job:** systems-qa-nightly-routing-review  
**Issue:** Job cannot execute with available tools

### Finding

The nightly routing QA review job (per SOUL.md "Your second designed job") is designed to work with wrapper scripts (`scripts/qa-read.sh`, `scripts/qa-propose.sh`), which must be executed via a shell tool. However:

1. `exec` tool is not available in my current tool set
2. TOOLS.md states: "`exec` is now denied — you have no ability to run arbitrary commands"
3. No alternative mechanism exists to invoke the wrapper scripts
4. All other available tools (`read`, `write`, etc.) are confined to workspace via `tools.fs.workspaceOnly`

The job is structurally blocked at step 1 (reading watermark via `scripts/qa-read.sh watermark`).

### Next Step

This requires Aaron to either:
- Re-enable `exec` tool with appropriate policy guardrails for this job, OR
- Redesign the job to work within currently available tools

No review data has been collected. The last successful watermark (1786093364000 / 2026-08-07 04:09:24 UTC) remains unchanged.
