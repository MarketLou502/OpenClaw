## Routing QA Digest — 2026-08-10

**Window:** Watermark 1786266041000 (2026-08-09 02:00:41 UTC) to run time 1786291200000 (2026-08-10 13:18 UTC).

**Totals by route:**
- `fast-path-router` (grammar): 2 turns (calendar-delete, food-log-recipe)
- `fast-path-food`: 0 turns (food logging went via router's food-fastpath, not standalone)
- `escalated-to-main`: 0 turns (no iMessage activity in window; last iMessage activity predates watermark)
- `router-unhandled`: 2 turns

**Escalation candidates:** None (no Main escalations to analyze).

**Grammar-gap candidates:**

1. **Candidate: "Remove Replication Link Move on Portinet for my Calendar"**
   - Route: router-unhandled
   - Reason: Matches DeleteCalendar intent pattern "(remove|delete|cancel) {title} (from|on) [my|the] calendar" but title contains multiple words/concepts. Current grammar parsed "Remove Replication Link Move on" and failed on "Portinet for my Calendar". 
   - Gap: The wildcard slot `{title}` should greedily match up to the next preposition. This is a wildcard issue (grammar already supports this pattern). **Not a grammar gap — this is a slot parsing edge case.**

2. **Candidate: "I just had two super lights"**
   - Route: router-unhandled
   - Reason: Attempted food log via "I just had two..." but failed both grammar match and food-fastpath. The phrase "super lights" doesn't match a known food recipe/USDA term.
   - Analysis: This is a legitimate food-log attempt that failed recognition. However, without domain knowledge of whether "super lights" is a real food item Aaron eats, I cannot safely propose a grammar or fastpath update. **Flagging as future manual specification work — not enough context to draft a fix.**

**Verdict:** No actionable grammar-gap candidates this cycle (first one is a deeper slot-parsing issue, second lacks domain confidence). No fast-path specs needed. Next run should confirm whether wildcard title slot behavior is intended or needs investigation in the grammar engine itself.
